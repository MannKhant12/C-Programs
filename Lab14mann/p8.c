#include <stdio.h>
void main(){
    int n,i;
    printf("Enter the size of Array => ");
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++){
        printf("enter the element %d => ",i+1);
        scanf("%d",&arr[i]);
    }
    

    }